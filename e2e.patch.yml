# E2E overlay: mount dsh-svg-motion + the driver that exercises it once.
# Loaded from the repo root (see README → Development).
- insert:
    - id: svg-motion
      name: ./lib/index.js
      inject: [tools]
    - id: svg-motion-e2e
      name: ./lib/e2e-driver.js
      inject: [tools]
